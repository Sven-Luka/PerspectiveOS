# Session-Notizen 2026-06-30 (Übergabe)

Arbeitskopie: `C:\Users\UserS2025\Documents\PerspectiveOS\PerspectiveOS_production_v0.2.0`
Thema heute: **Bahnhof-Referenzen** ingestiert + **Bild-Backend von gpt-image-1 auf Flux/RunPod umgestellt** + **eigene Persona-LoRA trainiert und live am Endpoint**.

---

## 1. Erledigt heute (alles committed auf `main`, NICHT gepusht)

- `85b13c6` **Bahnhof-Location-Referenzen** (7 Fotos → `assets/references/locations/bahnhof/`) + Scorer-Regel in `reference_selector.py`.
- `daa8e6e` **Umschaltbares Bild-Backend** `studio/image/backends/` (OpenAI gpt-image-1 ODER RunPod/Flux). Default bleibt openai.
- `a8dc1f7` **LoRA-Hook** im Flux-Backend + **Dataset-Builder** `studio/training/build_lora_dataset.py`.
- `1d8dfd5` **Trainings-Runbook** `FLUX_PHASE3_TRAINING_RUNBOOK.md` + **ai-toolkit-Config** `studio/training/ai_toolkit_jona_v1.yaml`.
- `c9a2ed8` **Orthese-Seite-Spiegel-Fix** `studio/image/postprocess.py` (opt-in `FIX_ORTHOSIS_SIDE=true`).

Pläne: `studio/FLUX_BACKEND_PLAN.md`, `studio/FLUX_PHASE3_LORA_PLAN.md`, `studio/FLUX_PHASE3_TRAINING_RUNBOOK.md`.

## 2. Persona-LoRA: FERTIG & LIVE

- **Datei:** `perspective_jona_v1.safetensors` (172 MB, 16 Bilder, 2500 Steps, ai-toolkit/Flux.1-dev, Trigger `j0na`).
- **Liegt auf dem Volume:** `/mnt/models/loras/perspective_jona_v1.safetensors` → der Endpoint nutzt sie.
- **Backup auf PC:** `studio/generated/_flux_poc/perspective_jona_v1.safetensors` (gitignored).
- **Echter Test-Render bestanden:** fotorealistisch, Outfit + blaue Knieorthese korrekt, Gesicht verborgen, **kein Moderation-Block**. Bilder: `studio/generated/_flux_poc/lora_render/`.

## 3. So rendert man mit der LoRA (von diesem PC)

```bash
cd studio
IMAGE_BACKEND=runpod_flux \
FLUX_LORA_NAME=perspective_jona_v1.safetensors \
FLUX_LORA_STRENGTH=0.9 \
# optional: FIX_ORTHOSIS_SIDE=true   (spiegelt, falls Orthese auf falschem Bein)
python ... (Backend get_backend("runpod_flux").generate(...))
```
In der App: `os.environ["IMAGE_BACKEND"]="runpod_flux"` setzt der Sidebar-Schalter.

## 4. RunPod-Fakten (WICHTIG, hat heute Stunden gekostet)

- Endpoint **`husky_crimson_hyena`** (`bkyqgi2m43kthj`), Image **`runpod/worker-comfyui:5.4.1-flux1-dev`**, Region **EU-SE-1**.
- Volume **`absent_fuchsia_smelt_volume`** (`rd26nueq18`, 50 GB, EU-SE-1), am Endpoint angehängt.
- ⚠️ **Auf einem POD ist das Volume unter `/mnt` gemountet, NICHT `/workspace`!** `/workspace` = lokale (flüchtige) Platte. LoRAs müssen nach **`/mnt/models/loras/`**.
- `idleTimeout=5s` → nächster Request startet frischen Worker, der die LoRA-Liste neu einliest.
- Dateitransfer Pod↔PC: **`runpodctl`** (lokal: `scratchpad/runpodctl.exe`; `send`/`receive` mit Code). Web-Terminal **frisst beim Einfügen Leerzeichen** → kurze Einzelzeilen verwenden.
- Training-Pod heute terminiert (Kosten gestoppt). Endpoint läuft serverless weiter.

## 5. KORREKTUR / Erkenntnis

Es gibt bereits **TENA/Windel-LoRAs** auf dem Volume: `/mnt/models/loras/tena_v2_0250/0500/0750/1000.safetensors` (früheres Windel-Training auf Flux). Also: Windel-1:1 auf Flux ist **schon vortrainiert**, nur noch nicht kombiniert.

## 6. OFFEN für morgen

1. **Windel auf Flux**: persona-LoRA **+** tena-LoRA **kombiniert** laden (zwei `LoraLoaderModelOnly`-Nodes in `runpod_flux.py`) → Person + TENA 1:1.
2. **App-Schalter** in `app.py`: LoRA an/aus + Strength + `FIX_ORTHOSIS_SIDE`.
3. **Persona-Feinschliff**: Strength 0.7–1.0 testen; ggf. v2-Dataset (mehr Knie-Nahaufnahmen, schärfere Captions) für stabilere Orthese-Position/Seite.
4. **Vision-Seiten-Erkennung** (`postprocess.ensure_orthosis_left`) am echten Render validieren (kostet OpenAI-Vision-Call).
5. **gpt-image-1-Pfad** bleibt parallel bestehen (Default) — entscheiden, wann Flux Default wird.

## 7. Sicherheit

`C:\Users\UserS2025\Documents\token_ki.txt` enthält RunPod-Key + HF-Token **im Klartext** → **rotieren** und als User-Env-Var ablegen.
