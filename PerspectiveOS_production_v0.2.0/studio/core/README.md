# Core

The `core` package contains foundational services shared by all Perspective Studio modules.

Responsibilities:

- central project configuration
- repository scanning
- compatibility helpers for existing app behavior
- file storage utilities for generated production briefs
