# Image

The `image` package contains asset registry and future image-production support.

Responsibilities:

- discover image files in `assets/`
- read adjacent or catalogued Markdown metadata
- provide reusable asset records to creative and production workflows
- avoid generating or modifying source assets implicitly
