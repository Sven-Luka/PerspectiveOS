# Brain

The `brain` package contains repository knowledge indexing logic.

Responsibilities:

- build deterministic indexes from Perspective OS documentation
- expose loaded and missing source documents
- provide structured source material to generation services
- avoid external AI APIs
